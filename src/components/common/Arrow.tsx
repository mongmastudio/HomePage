/*
  Tiny visual arrow span used inside .btn and community-list items.
  Matches the original `<span class="arr">→</span>` micro-component.
*/
export default function Arrow() {
  return <span className="arr">→</span>;
}
